start: sequence

sequence: element+
?element: repeated
    | simple

repeated: simple "*" INT

?simple: atom
    | group
    | division

atom: INT
group: INT "[" sequence "]"
division: INT "/" INT

%import common.INT
%import common.WS
%ignore WS
